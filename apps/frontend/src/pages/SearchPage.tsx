/** SearchPage — Global search results. Full implementation in Task-003. */
export default function SearchPage() {
  return (
    <section className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">
      <h1 className="text-3xl font-bold">Search Tools</h1>
      <p className="mt-4" style={{ color: 'var(--color-muted-foreground)' }}>
        Search interface — implementation in Task-003.
      </p>
    </section>
  )
}
